<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <% end %>
</div>


<div>
  <label><%=link_to "New Student", new_student_path%></label>

</div>
